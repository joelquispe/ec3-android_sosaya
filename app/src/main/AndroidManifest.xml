<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.example.joel_sq">

    <application
            android:name=".Configuracion.AppData"
            android:allowBackup="true"

            android:fullBackupContent="@xml/backup_rules"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/Theme.JoelSQ"
            tools:targetApi="31">
        <activity
                android:name="com.example.joel_sq.FrmProveedor"
                android:exported="false"
                android:label="@string/title_activity_frm_proveedor"
                android:theme="@style/Theme.JoelSQ.NoActionBar"/>
        <activity
                android:name="com.example.joel_sq.MainActivity"
                android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>